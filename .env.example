# ═══════════════════════════════════════════════════════
# LexiBel — Environment Variables Template
# Copy to .env and fill in the values
# ═══════════════════════════════════════════════════════

# ═══ Core Database ═══
DATABASE_URL=postgresql+asyncpg://lexibel:lexibel_dev_2026@localhost:5434/lexibel
DATABASE_SYNC_URL=postgresql://lexibel:lexibel_dev_2026@localhost:5434/lexibel

# ═══ Redis ═══
REDIS_URL=redis://localhost:6381/0

# ═══ JWT Auth ═══
JWT_SECRET_KEY=your-secret-key-here-change-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ═══ OAuth Encryption ═══
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
OAUTH_ENCRYPTION_KEY=your-32-byte-encryption-key-here

# ═══ Google Workspace (Gmail, Drive, Calendar) ═══
# Create at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://lexibel.clixite.cloud/api/v1/oauth/google/callback

# ═══ Microsoft 365 (Outlook, OneDrive, SharePoint) ═══
# Register at: https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
MICROSOFT_REDIRECT_URI=https://lexibel.clixite.cloud/api/v1/oauth/microsoft/callback
MICROSOFT_TENANT_ID=common

# ═══ LLM Providers (at least 1 required for AI features) ═══
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
MISTRAL_API_KEY=
DEEPSEEK_API_KEY=

# ═══ Ringover (telephony) ═══
RINGOVER_API_KEY=
RINGOVER_WEBHOOK_SECRET=

# ═══ Plaud (audio recording) ═══
PLAUD_API_KEY=

# ═══ MinIO (S3-compatible storage) ═══
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=lexibel

# ═══ Qdrant (vector search) ═══
QDRANT_URL=http://localhost:6333

# ═══ Neo4j (graph database) ═══
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=lexibel2026

# ═══ Frontend (Next.js) ═══
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
NEXTAUTH_SECRET=change-me-32-chars-min-for-production
NEXTAUTH_URL=https://lexibel.clixite.cloud
AUTH_TRUST_HOST=true
API_URL_INTERNAL=http://api:8000/api/v1

# ═══ Docker Compose ═══
POSTGRES_PASSWORD=lexibel_dev_2026
CORS_ORIGINS=http://localhost:3000,https://lexibel.clixite.cloud
SECRET_KEY=dev-secret-change-me-in-production
