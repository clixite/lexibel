# LexiBel Environment Variables
# Copy to .env and fill in real values. NEVER commit .env.

# ═══════════════════════════════════════════════════════════
# CORE SERVICES
# ═══════════════════════════════════════════════════════════

# PostgreSQL
POSTGRES_PASSWORD=change_me_strong_password_32chars
DATABASE_URL=postgresql+asyncpg://lexibel:${POSTGRES_PASSWORD}@localhost:5432/lexibel

# MinIO (S3-compatible object storage)
MINIO_ROOT_USER=lexibel
MINIO_ROOT_PASSWORD=change_me_strong_password_32chars
MINIO_ENDPOINT=localhost:9000
MINIO_BUCKET=lexibel-documents

# Redis (caching + pub/sub)
REDIS_URL=redis://localhost:6379/0

# Qdrant (vector database for RAG)
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=

# Neo4j (knowledge graph)
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=change_me_strong_password

# JWT Signing Key (generate: openssl rand -hex 64)
SECRET_KEY=change_me_generate_with_openssl_rand_hex_64

# OAuth Encryption Key (generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
OAUTH_ENCRYPTION_KEY=change_me_generate_with_fernet

# Environment
ENVIRONMENT=development

# ═══════════════════════════════════════════════════════════
# OAUTH & INTEGRATIONS
# ═══════════════════════════════════════════════════════════

# Google OAuth (Gmail + Google Calendar)
# Get credentials: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/callback/google
# Scopes: https://www.googleapis.com/auth/gmail.readonly https://www.googleapis.com/auth/calendar.readonly

# Microsoft OAuth (Outlook + Microsoft Calendar)
# Get credentials: https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps/ApplicationsListBlade
MICROSOFT_CLIENT_ID=your-azure-app-client-id
MICROSOFT_CLIENT_SECRET=your-azure-app-client-secret
MICROSOFT_TENANT_ID=common
MICROSOFT_REDIRECT_URI=http://localhost:3000/api/auth/callback/microsoft
# Scopes: Mail.Read Calendars.Read

# ═══════════════════════════════════════════════════════════
# TELEPHONY & TRANSCRIPTION
# ═══════════════════════════════════════════════════════════

# Ringover (VoIP telephony integration)
# Get API key: https://dashboard.ringover.com/settings/api
RINGOVER_API_KEY=your-ringover-api-key
RINGOVER_API_BASE_URL=https://public-api.ringover.com/v2
RINGOVER_WEBHOOK_SECRET=change_me_webhook_secret_for_hmac

# Plaud.ai (AI meeting transcription)
# Get API key: https://plaud.ai/api
PLAUD_API_KEY=your-plaud-api-key
PLAUD_API_BASE_URL=https://api.plaud.ai/v1
PLAUD_WEBHOOK_SECRET=change_me_webhook_secret_for_hmac

# ═══════════════════════════════════════════════════════════
# AI SERVICES
# ═══════════════════════════════════════════════════════════

# OpenAI (GPT-4, Whisper, Embeddings)
# Get API key: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-openai-api-key

# vLLM (optional - local LLM inference)
VLLM_ENDPOINT=http://localhost:8100/v1
VLLM_MODEL=mistralai/Mistral-7B-Instruct-v0.2

# ═══════════════════════════════════════════════════════════
# BELGIAN E-GOVERNMENT
# ═══════════════════════════════════════════════════════════

# e-Deposit (Belgian court system integration)
# Get credentials from your Barreau/Orde van Advocaten
DPA_CERTIFICATE_PATH=/path/to/your/certificate.p12
DPA_CERTIFICATE_PASSWORD=your-cert-password
DPA_ENVIRONMENT=acceptance  # acceptance | production

# Peppol (Belgian e-invoicing network)
PEPPOL_ENDPOINT_ID=9999:BE0123456789  # Your Peppol participant ID
PEPPOL_ACCESS_POINT_URL=https://your-access-point.peppol.eu

# ═══════════════════════════════════════════════════════════
# FRONTEND
# ═══════════════════════════════════════════════════════════

# Next.js
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
NEXT_PUBLIC_APP_NAME=LexiBel
NEXTAUTH_SECRET=change_me_generate_with_openssl_rand_hex_32
NEXTAUTH_URL=http://localhost:3000

# ═══════════════════════════════════════════════════════════
# MONITORING & OBSERVABILITY (Optional)
# ═══════════════════════════════════════════════════════════

# Sentry (error tracking)
SENTRY_DSN=

# PostHog (product analytics)
POSTHOG_API_KEY=
POSTHOG_HOST=https://app.posthog.com

# ═══════════════════════════════════════════════════════════
# DEPLOYMENT (Production only)
# ═══════════════════════════════════════════════════════════

# Domain
DOMAIN=lexibel.clixite.cloud

# CORS
ALLOWED_ORIGINS=https://lexibel.clixite.cloud,http://localhost:3000

# SSL/TLS (Let's Encrypt)
LETSENCRYPT_EMAIL=nicolas@clixite.be
